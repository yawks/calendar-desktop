{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Calendar app",
  "windows": ["main", "calendar"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window"
  ]
}
