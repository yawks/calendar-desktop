import { createContext, ReactNode, useContext, useEffect, useMemo, useState } from 'react'; export interface MailNotificationSettings { enabled: boolean; } type NotificationPermissionState = NotificationPermission | 'unsupported'; const STORAGE_KEY = 'courrier-mail-notifications-v1'; const MailNotificationContext = createContext<{ settings: MailNotificationSettings; supported: boolean; permission: NotificationPermissionState; enable(): Promise<boolean>; disable(): void; } | null>(null); const notificationSupported = () => 'Notification' in globalThis; const savedSettings = (): MailNotificationSettings => { try { return { enabled: JSON.parse(localStorage.getItem(STORAGE_KEY) ?? '{"enabled":false}').enabled === true }; } catch { return { enabled: false }; } }; export function MailNotificationProvider({ children }: { children: ReactNode }) { const [settings, setSettings] = useState(savedSettings); const supported = notificationSupported(); const [permission, setPermission] = useState<NotificationPermissionState>(() => supported ? Notification.permission : 'unsupported'); useEffect(() => { localStorage.setItem(STORAGE_KEY, JSON.stringify(settings)); }, [settings]); useEffect(() => { const refreshPermission = () => setPermission(notificationSupported() ? Notification.permission : 'unsupported'); globalThis.addEventListener('focus', refreshPermission); return () => globalThis.removeEventListener('focus', refreshPermission); }, []); const value = useMemo(() => ({ settings, supported, permission, enable: async () => { if (!notificationSupported()) return false; const nextPermission = await Notification.requestPermission(); setPermission(nextPermission); const enabled = nextPermission === 'granted'; setSettings({ enabled }); return enabled; }, disable: () => setSettings({ enabled: false }), }), [settings, supported, permission]); return <MailNotificationContext.Provider value={value}>{children}</MailNotificationContext.Provider>; } export function useMailNotificationsSettings() { const context = useContext(MailNotificationContext); if (!context) throw new Error('useMailNotificationsSettings must be used inside MailNotificationProvider'); return context; }
